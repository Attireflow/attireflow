import classes from "./landing.module.css";

function Landing() {
  return (
    <main className={classes.home}>
      <div className={`${classes.hero_section} header-background`}></div>
    </main>
  );
}

export default Landing;
